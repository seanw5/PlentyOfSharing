# Copy this file to .env (for local `vercel dev`) and fill in your real Neon
# connection string. Never commit the real .env file — it's already gitignored.
#
# Get this from the Neon console: your project → Connection Details →
# "Connection string" (use the pooled connection string).
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
